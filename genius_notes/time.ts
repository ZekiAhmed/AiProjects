const time = "00:55:54";

const time2 = "1:07:03";

// To use gemini cli use the following command
// gemini

// To install it use the following command
// npm install -g @google/gemini-cli
// To run it use the following command
// gemini --model gemini-1.5-flash-latest --key YOUR_API_KEY --prompt "What is the capital of France?" --output json
// To run it with a file use the following command
// gemini --model gemini-1.5-flash-latest --key YOUR_API_KEY --file ./path/to/file.txt --output json
// To run it with a file and a prompt use the following command
// gemini --model gemini-1.5-flash-latest --key YOUR_API_KEY --file ./path/to/file.txt --prompt "What is the capital of France?" --output json
// To run it with a file and a prompt and a temperature use the following command
// gemini --model gemini-1.5-flash-latest --key YOUR_API_KEY --file ./path/to/file.txt --prompt "What is the capital of France?" --temperature 0.5 --output json
// To run it with a file and a prompt and a temperature and a max tokens use the following command
// gemini --model gemini-1.5-flash-latest --key YOUR_API_KEY --file ./path/to/file.txt --prompt "What is the capital of France?" --temperature 0.5 --max-tokens 100 --output json
